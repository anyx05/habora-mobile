import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — feature tests live in their respective feature directories.
  testWidgets('placeholder test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
